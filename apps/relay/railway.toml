[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "package.json", "tsconfig.json", "Dockerfile", "drizzle.config.ts", "entrypoint.sh"]

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
