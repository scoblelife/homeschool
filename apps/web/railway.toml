[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @homeschool/design-tokens build && pnpm --filter @homeschool/web build"

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
