#!/usr/bin/env sh

# Lint staged files (eslint + prettier)
npx lint-staged

# Run unit tests (exclude integration tests that need external services)
pnpm --filter @homeschool/desktop exec vitest run --exclude '**/integration.test.*' --exclude '**/e2e/**'
